{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run generate"
  },
  "deploy": {
    "startCommand": "npx serve .output/public",
    "restartPolicyType": "ON_FAILURE"
  }
}